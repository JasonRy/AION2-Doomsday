# AION2-末日基地 开发日志

## 2026-03-24

### fix: 云函数 getOpenid 上传失败

- `cloudfunctions/getOpenid/` 缺少 `node_modules`，依赖 `wx-server-sdk` 未安装
- 解决方案：在微信开发者工具中右键选择「上传并部署：云端安装依赖」，或本地执行 `npm install` 后再上传

---

### fix: 创建房间时始终提示"请填写完整信息"

**问题一：校验逻辑错误（room.js）**

```js
// 错误：&& 导致三个字段同时为空才拦截
if (!form.name && !form.dungeonName && !form.raidTime)

// 修复：任意字段为空即拦截
if (!form.name || !form.dungeonName || !form.raidTime)
```

**问题二：输入事件未绑定（room.js）**

- `room.wxml` 所有输入框使用 `bindinput="onInput"`，但 `room.js` 只有 `inputName`、`inputDungeon` 等各自方法，缺少 `onInput`
- 导致输入内容完全未更新到 `form`，提交时字段全为空
- 修复：新增统一的 `onInput` 方法，通过 `data-field` 动态更新对应字段

```js
onInput(e) {
  const field = e.currentTarget.dataset.field
  const value = field === 'maxPlayers' ? parseInt(e.detail.value) : e.detail.value
  this.setData({ [`form.${field}`]: value })
}
```

---

### fix: 房间列表卡片 UI 对齐

- `index.wxss` `.room-name` 缺少宽度限制，长文字会挤压状态标签
- 修复：`.room-name` 加 `flex: 1 + min-width: 0 + overflow: ellipsis`；`.room-status` 加 `flex-shrink: 0`
- 新增 `.room-top` 底部分隔线，区分标题区与详情区

---

### fix: 所有页面输入框 placeholder 被遮挡

- `<input>` 组件在小程序中 `box-sizing: border-box` 有时不生效，`width: 100%` 加上 padding 导致实际宽度溢出，placeholder 被裁切
- 涉及页面：`room.wxss`、`signup.wxss`
- 修复方式：改为 `width: calc(100% - {左右padding之和})`，`box-sizing: content-box`

| 文件 | padding | 修复后 width |
|---|---|---|
| `room.wxss` | 左右各 30rpx | `calc(100% - 60rpx)` |
| `signup.wxss` | 左右各 24rpx | `calc(100% - 48rpx)` |

---

### fix: 全局背景色与深色主题冲突

- `app.wxss` 中 `page` 背景为 `#f6f6f6`（浅灰），页面滚动到底时露出白底
- 修复：改为 `#1a1a2e`，与全局深色主题一致

---

### feat: 开团时间改为日期+时间选择器

- `room.wxml` 将"开团时间"文本输入框替换为日期 picker + 时间 picker 并排布局
- `room.js` 新增 `onDateChange`、`onTimeChange` 事件，合并后存入 `form.raidTime`（格式：`YYYY-MM-DD HH:MM`）
- 默认值：日期为当天，时间为 20:00
- `room.wxss` 新增 `.picker-row`、`.picker-box` 样式

---

### fix: 房间列表卡片排布调整

- 卡片改为纵向排列：第一行房间名，第二行状态标签，第三行起副本信息
- `index.wxml` 拆分 `.room-top` 结构，状态标签改为 `<view>` 支持 `inline-flex` 垂直居中
- `index.wxss` 移除原 flex 横排，改用 `display: inline-flex` 居中状态文字，`.room-info` 加顶部分隔线

---

### feat: 副本名称改为选择器，新增种族/服务器/类型/职业联动选择

- `room.wxml/js` 副本名称改为下拉选择器，选项：深淵重鑄：盧德萊、侵蝕淨化所
- `signup.wxml/js` 报名表单新增种族（天族/魔族）+ 服务器联动选择器，并排显示（种族2:服务器3比例）
- 类型（输出/辅助/防御）+ 职业联动：输出→剑星/弓星/杀星/魔道星/精灵星，辅助→治愈星/护法星，防御→守护星
- 所有联动选择在切换上级时自动重置下级，并加入必填校验

---

### feat: 队员列表新增复制名称功能

- 每张队员卡片底部新增全宽「复制名称」按钮
- 复制格式：`角色名[服务器前两字]`，如 `艾拉[伊斯]`
- 使用 `wx.setClipboardData`，复制成功后 toast 提示

---

### fix: 队员列表卡片超出界面

- `.card-left` 加 `flex: 1; min-width: 0`，`.card-info` 加 `flex: 1; min-width: 0`
- 所有文字行加 `overflow: hidden; text-overflow: ellipsis; white-space: nowrap`
- 卡片展示新增字段：种族·服务器，类型·职业·装等

---

### fix: 踢人按钮冒泡误触

- `bindtap` 改为 `catchtap`，阻止事件冒泡触发父级 enterRoom

---

### feat: 删除队伍功能

- 仅队长可见，位于队伍名称右侧圆形红色按钮
- 点击弹出确认框，确认后删除数据库记录并刷新列表

---

### style: 全局 UI 优化

- 所有页面容器 padding 收至 12rpx，`app.wxss` 去除 `align-items: center` 避免内容收窄
- 队伍卡片（room-header）改为金色描边 + 深金渐变背景，与队员卡片（深蓝色）风格区分
- 顶部导航栏：队伍列表页改为「队伍列表」，详情页改为「队伍详情」
- 房间名称→队伍名称，副本名称→副本选择（room.wxml）

---

### chore: 新建开发分支

- 分支命名规则：`{类型}/{日期}`，例如 `dev/20260324`、`feat/20260324-xxx`
- 当前开发分支：`dev/20260324`

---

### feat: 报名/登记页新增简繁转换

- 报名（signup）和队员登记（register）的角色名输入框右侧新增「简/繁」开关
- 切换到「繁」时：已输入内容立即转换，输入中实时转换；切回「简」时还原原始文字
- 转换方案：提取 opencc-js 的 STCharacters 字典（约 32KB）生成本地工具文件 `utils/s2t.js`，无网络请求，即时响应
- 新增云函数 `convertTW`（备用，当前未被调用）

---

### feat: 新增「我的角色」功能

- 首页新增入口按钮「⚔️ 我的角色」
- 新建页面 `pages/myCharacters/myCharacters`，支持：
  - 添加角色：种族/服务器/角色名（含简繁切换）/类型/职业/装等/备注
  - 编辑角色：点卡片底部「编辑」预填表单，保存后刷新列表
  - 删除角色：弹确认框后删除，云数据库集合 `myCharacters`
- 报名页新增「从我的角色」按钮，选择角色后自动填入报名表单，仍可修改后提交
- 报名按钮区布局调整：「我要报名」独占一行，「从我的角色」与「从登记列表」并排于第二行

---

### feat: 移除测试数据模块

- 删除首页「🧪 填充测试数据」按钮及 `seedData()` 方法
- `cloudfunctions/seedData/` 代码保留（云端数据不受影响）

---

### chore: 新增 .gitignore

- 排除所有 `node_modules/` 目录及 `miniprogram/miniprogram_npm/`

---

## 2026-03-24（续）

### feat: 玩家名册筛选与排序

- 顶部新增种族 / 区 / 职业三栏筛选 picker，支持重置
- 默认排序：魔族 → 天族，同种族内按区名字母序
- 新增「共 N 名」结果统计，无匹配时提示「无匹配结果」
- 修复：区、职业 picker 展开后无文本，原因是 WXML 不支持 `.concat()`，改为 JS 预计算数组 `classFilterOptions`、`jobClassFilterOptions`

---

### feat: 玩家名册「我的角色优先」排序

- 一级排序：当前用户自己的角色显示在最前面
- 二级排序维持原有逻辑（魔族 → 天族 → 区名）
- 因 `checkIdentity`（云函数）与 `loadMembers` 并发，在 `checkIdentity` 回调中若成员已加载则补一次重排

---

### feat: 在队中状态检测（我的角色 & 玩家名册）

- 角色已报名任意队伍时，卡片显示「在【队伍名称】中」角标及半透明样式
- 编辑 / 删除 / 踢出操作触发弹框：「请先脱离队伍，是否前往所在队伍？」，确认跳转至对应队伍详情页
- 选择模式下在队中的角色不可被选取
- 查询方式：`signups` 按角色名列表查询（`charName in [...]`），避免因队长代报名导致 `_openid` 不匹配的漏检
- 房间不存在时过滤孤立 signup 记录，避免误判「在队中」

---

### feat: 删除队伍级联清理 signup 记录

- 新增云函数 `deleteRoom`，使用 admin 权限：
  1. 校验调用者为房间创建者（`creatorOpenid` 或 `_openid` 匹配）
  2. 批量删除该房间所有 `signups` 记录
  3. 删除房间本身
- 解决删除后角色仍显示「在队中」的问题

---

### feat: CD 周期自动解锁 + 队伍结束功能

**问题背景**

signup 记录不会自动消失，导致角色参加上一个 CD 的团后被旧记录锁住，无法加入新 CD 队伍。

**解决方案：A（时间过期）+ B（手动结束）**

- **时间过滤**：`loadMySignups` / `loadSignedUpMap` 构建在队中 map 时，检查 `raidTime + 6小时 < 当前时间`，过期的 signup 不计入，角色自动解锁
- **手动结束**：队伍详情页队长工具栏新增「结束本次」按钮，确认后调用 `deleteRoom` 云函数级联删除队伍及所有 signup，立即解锁所有角色
- **首页视觉**：过期队伍卡片半透明 + 显示「⌛ 已过期」标签，引导队长及时清理

**数据流**

1. 团结束 → 队长点「结束本次」→ 所有 signup 立即删除 → 角色当场解锁
2. 若队长未操作 → raidTime + 6h 后角色自动解锁，旧队伍卡片降级显示直到手动删除

---

### fix: 多处文案与跳转修正

- 首页主标题去除 `⚔️` 图标，副标题改为「圣域征召令」
- 创建副本页：内页标题改为「创建队伍」，顶部导航栏改为「创建」，提交按钮同步更新
- `_promptInTeam` 跳转参数由 `?id=` 修正为 `?roomId=`（与报名页 `options.roomId` 一致）
