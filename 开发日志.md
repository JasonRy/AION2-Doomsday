# AION2-末日基地 开发日志

## 2026-05-15

### version2.0: Web 角色查询与属性分析

**本版定位**

- 将 `web/` 从静态原型推进为可实际查询的角色资料页
- 当前核心范围：角色查询、搜索结果、角色详情、装备展示、主要能力值分析、深浅色切换
- 本版重点是建立可维护的数据解析口径，避免后续扩展 PVE、首领、百分比等模块时互相污染

**feat: 角色查询 Web 化**

- 新增本地 Node 代理：浏览器请求 `/api/*`，由 `web/server.js` 转发到 NCSoft 公开 API
- 代理请求不携带 `Origin`，避免官方接口返回 `403`
- 支持种族筛选、服务器筛选、角色名查询、搜索结果列表、查询历史
- 角色名输入保留简繁转换入口，优先走 `/api/convert`，失败时使用本地兜底转换

**feat: 角色详情结构**

- 详情页拆为两个 tab：`装备`、`属性分析`
- 角色头像放大，保留战力、道具等级、角色等级等核心摘要
- 装备页按模块展示：装备、卡牌、技能
- 装备网格为 2*N；卡牌模块独立展示，并在卡牌模块下方汇总套装属性
- 技能按主动、被动、烙印类型分块展示

**feat: 装备显示规则**

- 装备卡片左上角显示装备 icon
- 顶部显示突破等级、强化等级、装备名称
- 装备名称按等级色显示：Epic `#ee6c2a`、Unique `#e9a43a`、蓝 `#3d94d8`、绿 `#52b35c`、白 `#cccccc`
- 名称下方左侧显示灵魂刻印词条，右侧显示魔石/灵石词条
- 魔石/灵石词条不重复显示“魔石/灵石”前缀，只显示属性名和值
- 没有魔石/灵石的部位不显示该栏，例如古文石、护身符
- 护身符排列到项链右侧；古文石和护身符归入装备模块

**feat: 属性分析 - 主要能力值**

- 攻击力分析改名为属性分析
- 当前“主要能力值”只展示 14 项：攻击力、额外攻击力、防御力、额外防御力、命中、额外命中、回避、额外回避、暴击、暴击抵抗、生命力、精神力、战斗速度、移动速度
- 每个属性项支持展开查看来源
- 来源一级显示来源对象，例如 `主手 +25 夔龍王短劍`
- 来源二级缩进合并显示组成：基础能力值、灵魂刻印、魔石、佩戴效果等

**fix: 14 项主要能力值字段修正（本次 session）**

- `额外防御力`：guards 扩展为 `/增加|額外|额外/`，修复 "額外防禦力" 被普通防御力兜底的误判
- `魔石命中`：魔石 `WeaponAccuracy`/`HitRate` 改走 `extraHit`，因为魔石只提供额外命中而非基础命中；`sourceType="magicstone"` 覆写
- `魔石回避`：魔石 `EvasionBase` 改走 `extraEvasion`，同上逻辑，id 路径与 name 路径均覆写
- `PVP暴击`：新增 `/PVP|PvP/` 名称排除，防止 PVP暴击 进入普通暴击分桶
- `生命力恢复`：guard 追加 `name.includes("恢復")`，称号/词条提供的 HP 恢复不再误入生命力
- `精神力消耗减少`：guard 追加 `/恢復|消耗/`，精神力消耗减少/恢复不进精神力
- `isPct`：移除 `name.includes("速度")` 判断，战斗速度/移动速度的整数值不再被误判为百分比而丢弃
- `statSecondList` 对象处理：原先 `addDesc` 只接受字符串，对象类条目静默丢弃；改为 `addStat` 分支处理带 id/value 的派生能力对象
- `战斗速度（属性转换）`：新增 `detailStatSecondary` 直读分支——遍历角色二级能力表，对 `CombatSpeed`/`MoveSpeed` 直接按 `stat.value` 写入；解决时间[希埃尔]等属性转换职业战斗速度不显示的问题
- `移动速度`：同上，`MoveSpeed` 亦受益于该分支
- `战斗速度/移动速度百分比`：`addValue` 新增 `allowPct` 豁免——`combatSpeed`/`moveSpeed` 的百分比值不再被入口 guard 丢弃，改为写入 `target.pct`；其余 12 项字段行为不变
- `feat: 百分比增加模块`：新增 `PCT_STAT_DEFS`（10 项），在 `calcAttributes` 内建立平行的 `pctValues` 管道；`addStat` 在 `matchMetric` 返回 null 时自动转发给 `addPctStat`，即原先被主能力值排除的词条（灵魂刻印、属性转化、称号、守护力）均自动路由到百分比分桶；渲染层新增"百分比增加"区块，仅显示 count > 0 的项
- `fix: 守护力百分比词条漏采`：`loadDaevanionEntries` 正则新增全部 pct 词条名（攻擊力增加、防禦力增加等 10 类），捕获组改为 `[\d.]+%?` 以兼容带 `%` 的值；修复前守护力提供的攻擊力增加% 无法进入百分比模块的问题
- `基礎能力值转化说明`：威力/敏捷/知識/體力/精確/意志各項对百分比属性的转化上限为 200 点 = 20%（换算 1 点 = 0.1%）；转化值由 `detailStatBasic` 的 `statSecondList` 直接给出，代码无需手动实现换算；最大值 200 点时对应的百分比增加为 20%

**feat: 其他手段模块（本次 session）**

- 新增 `OTHER_STAT_DEFS`（14 项）：多段打擊擊中/抵抗、鐵壁貫穿/鐵壁、再生貫穿/再生、完美/完美抵抗、強擊/強擊抵抗、後方暴擊/抵抗、格擋貫穿/格擋
- 所有 id 均为推测值，尚未从 API 实际响应确认；字段能否命中完全依赖繁简中文名称匹配，待查到真实 id 后补入
- 管道与百分比增加完全并列：`addStat` → `matchMetric` 返回 null → `addOtherStat` → `matchOtherMetric`
- `matchOtherMetric` 设有防短名误匹配的 guard（详见下方"三段路由注意事项"）
- **网格排布**：14 项按 [进攻, 对应防御] 交替排列，2 列 grid 自然对齐成行对；进攻在左（击中/贯穿/主动），防御在右（抵抗/本体/被动）；格挡贯穿（攻）对格挡（守），鐵壁贯穿（攻）对鐵壁（守），其余同理
- **全量显示**：其他手段始终渲染全部 14 项，有数据的正常显示，无数据的加 `attr-card--empty`（`opacity:0.38` + `pointer-events:none`），保证配对位置不因数据缺失而错位；标题计数只计 count > 0 的项
- **守护力正则**：`loadDaevanionEntries` 正则已补入全部 14 项繁简名称；守护力来源的其他手段词条现可被正确采入
- **isPct 标志**：`OTHER_STAT_DEFS` 每项带 `isPct` 字段；格擋貫穿、格擋、後方暴擊 = `false`（整数），其余 11 项 = `true`（百分比）
- **addOtherStat 值类型检测**：优先检测 API 原始值是否含 `%`（`apiHasPct`），有则直接走 pct；无则按 `def.isPct` 决定——`true` 强制写 pct（前端自动加 `%`），`false` 写 flat（整数显示）

**⚠ 三段路由注意事项（重要，每次新增模块都要看）**

属性分析现在有三段平行管道，路由顺序在 `addStat` 里固定为：

```
① isPct 且 matchPctMetric 命中  →  addPctStat（百分比增加）
② matchMetric 命中              →  addValue（主要能力值）
③ 其余                          →  addOtherStat（其他手段）
```

**不要动 `matchMetric` 的 guard 来解决新模块的路由问题。**
- `matchMetric` 只负责主要能力值的取舍，改它会影响已经校准的 14 项数值
- 新模块（百分比增加、其他手段）的路由靠 `addStat` 里的早期拦截决定，与 `matchMetric` 无关
- 验证路径：先确认主要能力值不变，再看新模块有无数据

**`isPct` 的判定逻辑决定走哪条路：**
- `String(value).includes("%") || String(extra).includes("%") || name.includes("增加")` → 视为百分比类
- 百分比类先走 `matchPctMetric`；如果 pct 定义里没有这个词条，会继续走 `matchMetric` 然后最终到 `addOtherStat`
- 纯整数值（无 % 无增加）直接从 ② 起路由

**`matchOtherMetric` 的 guard 规则：**
- `格擋`/`鐵壁`/`再生`：名称含"贯穿"时排除，防止短名匹配到贯穿版
- `完美`/`強擊`/`後方暴擊`：名称含"抵抗"时排除，防止短名匹配到抵抗版
- 如新增短名词条，必须同步评估是否需要对应 guard

**基础属性派生的 20% 上限：**
- `detailStatBasic`（威力/敏捷/知識/體力/精確/意志）的 `statSecondList` 对百分比管道有 `cap=20` 限制
- 实现在 `addPctStat` 的 `allowed = min(num, max(0, cap - source.pct))` 逻辑
- 上限是 per-source per-bucket，即每个基础属性对每个百分比分桶最多贡献 20%
- `detailStatSecondary`（灵魂刻印派生等）不受此限制

**计算口径（非常重要）**

- `攻擊力` 只统计明确的普通攻击力，不包含额外攻击力、PVE攻击力、首领攻击力、暴击攻击力、攻击力增加%、伤害增幅
- `額外攻擊力` 只统计明确的额外攻击力
- `PVE攻擊力`、`首領攻擊力` 当前不得出现在主要能力值中，后续会单独设计版面
- 百分比类属性当前不进入主要能力值，后续单独做百分比模块
- 守护力来源按“守护力”一级归并，二级列出各守护力名称

**fix: WeaponFixingDamage 口径确认**

- 以 `憶雨[奧達爾]` 主手武器为基准，主手基础攻击力确认使用：`round((minValue + value) / 2) + extra`
- 当前该主手基础攻击力为 937
- 当前 `憶雨[奧達爾]` 主要能力值中普通攻击力为 4077，额外攻击力为 456

**feat: 翅膀佩戴效果临时数据源**

- 官方角色装备接口当前只返回翅膀身份信息：`id/name/enchantLevel/grade/icon`，未返回佩戴效果
- 新增 `web/public/data/wing-effects.json`，按 wing id 补充翅膀佩戴效果，方便后续扩展多个翅膀
- 已补 `空虛塔里斯拉翅膀`：额外攻击力 +60、额外防御力 +400、额外命中 +35、冷却时间减少 +4%
- 字段尽量沿用当前官方 stat id：`AdditionalWeaponFixingDamage`、`AdditionalDefense`、`Accuracy`
- `CoolTimeReduce` 暂不能确认官方字段名，只作为临时语义字段，后续如发现官方字段需替换


**字段取值说明（给下次开发用）**

主能力值只从 `PRIMARY_STAT_DEFS` 当前 14 个定义里渲染。新增字段前先确认是否属于当前主能力值；不属于就先解析但不要展示，等独立版面。

| 展示字段 | 可接受官方 id / 词条 | 当前取值来源 | 明确排除 |
|---|---|---|---|
| 攻击力 | `WeaponFixingDamage` / 纯 `攻擊力`、`攻击力` | 装备基础、装备强化/突破、灵魂刻印、魔石 | `額外攻擊力`、`PVE攻擊力`、`首領攻擊力`、`暴擊攻擊力`、`攻擊力增加%`、`DamageRatio`、`傷害增幅` |
| 额外攻击力 | `AdditionalWeaponFixingDamage`、`AdditionalAttack`、`AdditionalDamage` / `額外攻擊力`、`额外攻击力` | 守护力、角色等级、翅膀佩戴效果、后续明确额外攻击力来源 | PVE、首领、普通攻击力、百分比攻击力 |
| 防御力 | `Defense` / `防禦力`、`防御力` | 装备基础、灵魂刻印、魔石 | `防禦力增加%`、额外防御力 |
| 额外防御力 | `AdditionalDefense` / `額外防禦力`、`额外防御力` | 翅膀佩戴效果、后续明确额外防御力来源 | 普通防御力、百分比防御力 |
| 命中 | `WeaponAccuracy`、`HitRate` / 纯 `命中` | 装备基础、灵魂刻印 | `額外命中`、`命中增加%`；**魔石命中 → 归到额外命中** |
| 额外命中 | `Accuracy` / `額外命中` | 魔石（含 WeaponAccuracy/HitRate id）、翅膀佩戴效果、后续明确额外命中来源 | 普通命中、百分比命中 |
| 回避 | `EvasionBase` / 纯 `迴避`、`回避` | 装备基础、灵魂刻印 | `額外迴避`、`迴避增加%`；**魔石回避 → 归到额外回避** |
| 额外回避 | `Evasion` / `額外迴避`、`迴避增加`、`回避增加` | 魔石（含 EvasionBase id）、后续明确额外回避来源 | 普通回避 |
| 暴击 | `Critical` / `暴擊`、`暴击` | 装备基础、灵魂刻印、魔石 | `暴擊抵抗`、`暴擊攻擊力`、`暴擊傷害` |
| 暴击抵抗 | `CriticalResist` / `暴擊抵抗`、`暴击抵抗` | 装备基础、灵魂刻印、魔石 | 普通暴击 |
| 生命力 | `HP` / `生命力`、`HP` | 装备基础、灵魂刻印、魔石 | `生命力增加%` |
| 精神力 | `MP` / `精神力`、`MP` | 装备基础、灵魂刻印、魔石 | `精神力增加%`、精神力消耗减少 |
| 战斗速度 | `CombatSpeed` / `戰鬥速度`、`战斗速度` | 装备词条、属性转换（`detailStatSecondary` 直读，来源标 `屬性轉換`）| 百分比值现写入 `pct`，前端渲染需同步支持 |
| 移动速度 | `MoveSpeed` / `移動速度`、`移动速度` | 装备词条、属性转换（同上）| 同上 |

来源归类规则：

- `mainStatsNormal` + `mainStatsExceed` 统一归到二级来源 `基础能力值`。`WeaponFixingDamage` 有 `minValue/value/extra` 时按 `round((minValue + value) / 2) + extra` 取值。
- `subStats` 归到二级来源 `灵魂刻印`。
- `magicStoneStat` 归到二级来源 `魔石`。
- `godStoneStat.statList` 归到二级来源 `灵石`，但只解析命中当前主能力值的明确词条。
- `detailStatBasic/detailStatSecondary` 里的派生百分比当前不进入主能力值。
- `detailStatSecondary` 直读分支：仅对 `CombatSpeed`/`MoveSpeed` 读取 `stat.value`，其余字段不走此路径，避免与装备来源重复叠加。
- `title.equipStatList` 当前只解析命中主能力值且非百分比的明确词条；暴击攻击力、伤害增幅不进攻击力。
- `daevanion.openStatEffectList` 一级来源固定为 `守护力`，二级列守护力名称。主能力值范围内的词条进主能力值；百分比类（攻擊力增加%等）进百分比增加模块；PVE/首领保留给未来独立模块。
- `petwing.wing` 官方接口当前只给身份信息；佩戴效果从 `web/public/data/wing-effects.json` 用 wing id 匹配。没有官方字段前，新增翅膀属性必须在 JSON 里标明是否为临时字段。

禁止兜底规则：

- 不要用“包含 攻擊力 三个字”来匹配普通攻击力，必须先排除 `額外攻擊力`、`PVE攻擊力`、`首領攻擊力`、`暴擊攻擊力`、`攻擊力增加`。
- 不要把任何 `%` 数值塞进当前主能力值。百分比等单独模块。
- 不要把 `PVE攻擊力`、`首領攻擊力` 加回主要能力值网格。它们以后有独立版面。
- 不要把用户口述的临时字段伪装成官方字段。不能确认官方 id 时，在 JSON 中保留语义 id，并在日志说明。

**known issue / TODO**

- LV45 额外攻击力当前按已确认值 61 处理，后续需要等级表或官方字段
- PVE攻击力、首领攻击力、百分比属性、冷却时间减少均等待独立版面，不要混入主要能力值
- 战斗速度 `detailStatSecondary` 直读值如含装备贡献（即为全量值）则会与装备来源重叠，待观察实测数据后确认是否需要拆分
- `OTHER_STAT_DEFS` 14 项的官方 id 全部为推测值，未经 API 验证；如页面上某项不显示，用 DevTools 查该装备/技能的原始字段名补入 `ids` 数组
- 若官方后续开放翅膀佩戴效果接口，优先替换 `wing-effects.json` 数据源，但保留按 wing id 匹配的接入方式
- 宠物理解度无公开接口，后续可用 `pet-comprehension.json` 补充，接入方式同翅膀
- 简繁转换当前仍有兜底字典限制，后续可替换为更完整的转换库

---

## 2026-03-26

### feat: 角色查詢頁——攻擊力分析系統

**攻擊力計算邏輯**

- 計算公式：固定攻擊力合計 × (1 + 攻擊力增加%) = 最終攻擊力
- 固定攻擊力來源：裝備本體 `(minValue + value) / 2`、強化加成 `extra`、突破加成、裝備詞條、魔石、神石、守護力額外攻擊力、角色等級基礎攻擊力
- 百分比來源：DamageRatio 本體、突破、詞條、魔石、神石、能力值附加描述、称号
- 正則同時兼容繁體（攻擊力）與簡體（攻击力）

**fix: 裝備接口 enchantLevel 參數**

- 修正為 `enchantLevel + exceedLevel` 之和，確保強化+突破後屬性正確返回

**fix: WeaponFixingDamage 基礎攻擊力公式**

- 非突破時改用 `(minValue + value) / 2`，消除接口返回值的膨脹誤差

**feat: 守護力額外攻擊力**

- boardId 取自 `info.daevanion.boardList[].id`（不再寫死 41-47）
- 解析 `openStatEffectList[].desc` 中的額外攻擊力數值，按板塊名稱分條記錄

**feat: 攻擊力分析 UI**

- 詳情頁新增摘要卡片（固定攻擊力 × 增加率 = 最終攻擊力），點擊展開全屏詳情疊層
- 詳情疊層按裝備槽位折疊分組，標題為裝備名稱，副標題為槽位名（主手/副手等）
- 每組顯示固定攻擊小計（金色）與百分比小計（藍色），展開後逐條列出來源
- 末尾追加守護力、能力值、称号三個獨立分組

**known issue / TODO**

- 角色等級基礎攻擊力（LV45 = 61）官方 API 暫未開放，目前寫死為 61
- 待接口開放後改為動態讀取並建立等級對照表（見 `charSearch.js` `_calcAttack` TODO 注釋）

---

## 2026-03-24

### fix: 云函数 getOpenid 上传失败

- `cloudfunctions/getOpenid/` 缺少 `node_modules`，依赖 `wx-server-sdk` 未安装
- 解决方案：在微信开发者工具中右键选择「上传并部署：云端安装依赖」，或本地执行 `npm install` 后再上传

---

### fix: 创建房间时始终提示"请填写完整信息"

**问题一：校验逻辑错误（room.js）**

```js
// 错误：&& 导致三个字段同时为空才拦截
if (!form.name && !form.dungeonName && !form.raidTime)

// 修复：任意字段为空即拦截
if (!form.name || !form.dungeonName || !form.raidTime)
```

**问题二：输入事件未绑定（room.js）**

- `room.wxml` 所有输入框使用 `bindinput="onInput"`，但 `room.js` 只有 `inputName`、`inputDungeon` 等各自方法，缺少 `onInput`
- 导致输入内容完全未更新到 `form`，提交时字段全为空
- 修复：新增统一的 `onInput` 方法，通过 `data-field` 动态更新对应字段

```js
onInput(e) {
  const field = e.currentTarget.dataset.field
  const value = field === 'maxPlayers' ? parseInt(e.detail.value) : e.detail.value
  this.setData({ [`form.${field}`]: value })
}
```

---

### fix: 房间列表卡片 UI 对齐

- `index.wxss` `.room-name` 缺少宽度限制，长文字会挤压状态标签
- 修复：`.room-name` 加 `flex: 1 + min-width: 0 + overflow: ellipsis`；`.room-status` 加 `flex-shrink: 0`
- 新增 `.room-top` 底部分隔线，区分标题区与详情区

---

### fix: 所有页面输入框 placeholder 被遮挡

- `<input>` 组件在小程序中 `box-sizing: border-box` 有时不生效，`width: 100%` 加上 padding 导致实际宽度溢出，placeholder 被裁切
- 涉及页面：`room.wxss`、`signup.wxss`
- 修复方式：改为 `width: calc(100% - {左右padding之和})`，`box-sizing: content-box`

| 文件 | padding | 修复后 width |
|---|---|---|
| `room.wxss` | 左右各 30rpx | `calc(100% - 60rpx)` |
| `signup.wxss` | 左右各 24rpx | `calc(100% - 48rpx)` |

---

### fix: 全局背景色与深色主题冲突

- `app.wxss` 中 `page` 背景为 `#f6f6f6`（浅灰），页面滚动到底时露出白底
- 修复：改为 `#1a1a2e`，与全局深色主题一致

---

### feat: 开团时间改为日期+时间选择器

- `room.wxml` 将"开团时间"文本输入框替换为日期 picker + 时间 picker 并排布局
- `room.js` 新增 `onDateChange`、`onTimeChange` 事件，合并后存入 `form.raidTime`（格式：`YYYY-MM-DD HH:MM`）
- 默认值：日期为当天，时间为 20:00
- `room.wxss` 新增 `.picker-row`、`.picker-box` 样式

---

### fix: 房间列表卡片排布调整

- 卡片改为纵向排列：第一行房间名，第二行状态标签，第三行起副本信息
- `index.wxml` 拆分 `.room-top` 结构，状态标签改为 `<view>` 支持 `inline-flex` 垂直居中
- `index.wxss` 移除原 flex 横排，改用 `display: inline-flex` 居中状态文字，`.room-info` 加顶部分隔线

---

### feat: 副本名称改为选择器，新增种族/服务器/类型/职业联动选择

- `room.wxml/js` 副本名称改为下拉选择器，选项：深淵重鑄：盧德萊、侵蝕淨化所
- `signup.wxml/js` 报名表单新增种族（天族/魔族）+ 服务器联动选择器，并排显示（种族2:服务器3比例）
- 类型（输出/辅助/防御）+ 职业联动：输出→剑星/弓星/杀星/魔道星/精灵星，辅助→治愈星/护法星，防御→守护星
- 所有联动选择在切换上级时自动重置下级，并加入必填校验

---

### feat: 队员列表新增复制名称功能

- 每张队员卡片底部新增全宽「复制名称」按钮
- 复制格式：`角色名[服务器前两字]`，如 `艾拉[伊斯]`
- 使用 `wx.setClipboardData`，复制成功后 toast 提示

---

### fix: 队员列表卡片超出界面

- `.card-left` 加 `flex: 1; min-width: 0`，`.card-info` 加 `flex: 1; min-width: 0`
- 所有文字行加 `overflow: hidden; text-overflow: ellipsis; white-space: nowrap`
- 卡片展示新增字段：种族·服务器，类型·职业·装等

---

### fix: 踢人按钮冒泡误触

- `bindtap` 改为 `catchtap`，阻止事件冒泡触发父级 enterRoom

---

### feat: 删除队伍功能

- 仅队长可见，位于队伍名称右侧圆形红色按钮
- 点击弹出确认框，确认后删除数据库记录并刷新列表

---

### style: 全局 UI 优化

- 所有页面容器 padding 收至 12rpx，`app.wxss` 去除 `align-items: center` 避免内容收窄
- 队伍卡片（room-header）改为金色描边 + 深金渐变背景，与队员卡片（深蓝色）风格区分
- 顶部导航栏：队伍列表页改为「队伍列表」，详情页改为「队伍详情」
- 房间名称→队伍名称，副本名称→副本选择（room.wxml）

---

### chore: 新建开发分支

- 分支命名规则：`{类型}/{日期}`，例如 `dev/20260324`、`feat/20260324-xxx`
- 当前开发分支：`dev/20260324`

---

### feat: 报名/登记页新增简繁转换

- 报名（signup）和队员登记（register）的角色名输入框右侧新增「简/繁」开关
- 切换到「繁」时：已输入内容立即转换，输入中实时转换；切回「简」时还原原始文字
- 转换方案：提取 opencc-js 的 STCharacters 字典（约 32KB）生成本地工具文件 `utils/s2t.js`，无网络请求，即时响应
- 新增云函数 `convertTW`（备用，当前未被调用）

---

### feat: 新增「我的角色」功能

- 首页新增入口按钮「⚔️ 我的角色」
- 新建页面 `pages/myCharacters/myCharacters`，支持：
  - 添加角色：种族/服务器/角色名（含简繁切换）/类型/职业/装等/备注
  - 编辑角色：点卡片底部「编辑」预填表单，保存后刷新列表
  - 删除角色：弹确认框后删除，云数据库集合 `myCharacters`
- 报名页新增「从我的角色」按钮，选择角色后自动填入报名表单，仍可修改后提交
- 报名按钮区布局调整：「我要报名」独占一行，「从我的角色」与「从登记列表」并排于第二行

---

### feat: 移除测试数据模块

- 删除首页「🧪 填充测试数据」按钮及 `seedData()` 方法
- `cloudfunctions/seedData/` 代码保留（云端数据不受影响）

---

### chore: 新增 .gitignore

- 排除所有 `node_modules/` 目录及 `miniprogram/miniprogram_npm/`

---

## 2026-03-24（续）

### feat: 玩家名册筛选与排序

- 顶部新增种族 / 区 / 职业三栏筛选 picker，支持重置
- 默认排序：魔族 → 天族，同种族内按区名字母序
- 新增「共 N 名」结果统计，无匹配时提示「无匹配结果」
- 修复：区、职业 picker 展开后无文本，原因是 WXML 不支持 `.concat()`，改为 JS 预计算数组 `classFilterOptions`、`jobClassFilterOptions`

---

### feat: 玩家名册「我的角色优先」排序

- 一级排序：当前用户自己的角色显示在最前面
- 二级排序维持原有逻辑（魔族 → 天族 → 区名）
- 因 `checkIdentity`（云函数）与 `loadMembers` 并发，在 `checkIdentity` 回调中若成员已加载则补一次重排

---

### feat: 在队中状态检测（我的角色 & 玩家名册）

- 角色已报名任意队伍时，卡片显示「在【队伍名称】中」角标及半透明样式
- 编辑 / 删除 / 踢出操作触发弹框：「请先脱离队伍，是否前往所在队伍？」，确认跳转至对应队伍详情页
- 选择模式下在队中的角色不可被选取
- 查询方式：`signups` 按角色名列表查询（`charName in [...]`），避免因队长代报名导致 `_openid` 不匹配的漏检
- 房间不存在时过滤孤立 signup 记录，避免误判「在队中」

---

### feat: 删除队伍级联清理 signup 记录

- 新增云函数 `deleteRoom`，使用 admin 权限：
  1. 校验调用者为房间创建者（`creatorOpenid` 或 `_openid` 匹配）
  2. 批量删除该房间所有 `signups` 记录
  3. 删除房间本身
- 解决删除后角色仍显示「在队中」的问题

---

### feat: CD 周期自动解锁 + 队伍结束功能

**问题背景**

signup 记录不会自动消失，导致角色参加上一个 CD 的团后被旧记录锁住，无法加入新 CD 队伍。

**解决方案：A（时间过期）+ B（手动结束）**

- **时间过滤**：`loadMySignups` / `loadSignedUpMap` 构建在队中 map 时，检查 `raidTime + 6小时 < 当前时间`，过期的 signup 不计入，角色自动解锁
- **手动结束**：队伍详情页队长工具栏新增「结束本次」按钮，确认后调用 `deleteRoom` 云函数级联删除队伍及所有 signup，立即解锁所有角色
- **首页视觉**：过期队伍卡片半透明 + 显示「⌛ 已过期」标签，引导队长及时清理

**数据流**

1. 团结束 → 队长点「结束本次」→ 所有 signup 立即删除 → 角色当场解锁
2. 若队长未操作 → raidTime + 6h 后角色自动解锁，旧队伍卡片降级显示直到手动删除

---

### style: 全局 UI 优化

- **种族色条**：魔族（`#8b3a4a` 暗红）/ 天族（`#3a6899` 冰蓝）左侧 4rpx 色条，应用于玩家名册、队伍名单、我的角色三处卡片
- **按钮点击反馈**：`app.wxss` 全局覆写 `.button-hover` 透明度为 0.72，所有按钮获得统一点击态
- **圆角统一**：输入框 10rpx → 12rpx（register、signup、myCharacters），状态角标 20rpx → 8rpx（registerList），取消按钮 10rpx → 12rpx
- **次要文字对比度**：全局 `#8888aa` → `#9999bb`，覆盖所有页面的标签、说明文字
- **首页空状态**：「暂无开放的副本」升级为多行样式，加入装饰符号 ✦ 与副标题「等待队长召集」，搭配字间距

---

### fix: 多处文案与跳转修正

- 首页主标题去除 `⚔️` 图标，副标题改为「圣域征召令」
- 创建副本页：内页标题改为「创建队伍」，顶部导航栏改为「创建」，提交按钮同步更新
- `_promptInTeam` 跳转参数由 `?id=` 修正为 `?roomId=`（与报名页 `options.roomId` 一致）




